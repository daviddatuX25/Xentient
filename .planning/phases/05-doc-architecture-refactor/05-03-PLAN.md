---
phase: 05-doc-architecture-refactor
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - NOTES.md
  - xentient.md
  - .planning/phases/02-harness-intelligence-layer/SUPERSEDED.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05
  - DOC-07

must_haves:
  truths:
    - "ROADMAP.md shows two tracks: Demo Track (frozen scope, Apr 24) and Platform Track (P1-P8, post-capstone)"
    - "NOTES.md is trimmed to append-only decision log with dated entries only; platform/pack/SDK content extracted to L2 specs"
    - "xentient.md shrunk from ~647 lines to ~200 lines as L0 narrative pitch"
    - "PROJECT.md reflects bridge-model vision, not old 'visual control plane' language"
    - "REQUIREMENTS.md has MEM-01/02/03 replaced with delegation reqs and HARN-01 rephrased as voice pipeline"
    - "Phase 2 directory has SUPERSEDED.md marker explaining the bridge reframe"
    - "STATE.md reflects Phase 5 as current focus with doc architecture refactor status"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Two-track roadmap with Demo Track and Platform Track"
      contains: "Demo Track"
    - path: ".planning/PROJECT.md"
      provides: "Updated project charter with bridge model"
      contains: "bridge"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with delegation reqs"
      contains: "MEM"
    - path: ".planning/STATE.md"
      provides: "Updated project state"
      contains: "Phase 5"
    - path: "NOTES.md"
      provides: "Trimmed append-only decision log"
      max_lines: 100
    - path: "xentient.md"
      provides: "Shrunk narrative pitch (~200 lines)"
      max_lines: 220
    - path: ".planning/phases/02-harness-intelligence-layer/SUPERSEDED.md"
      provides: "Marker explaining bridge reframe superseded phase 2"
      contains: "SUPERSEDED"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "docs/VISION.md"
      via: "Platform Track P1-P8 references"
      pattern: "P1.*P8|Platform Track"
    - from: ".planning/PROJECT.md"
      to: "docs/VISION.md"
      via: "Core value references bridge model"
      pattern: "bridge|IoT terminal"
---

<objective>
Restructure all existing project documentation to align with the bridge-model vision. Rewrite ROADMAP.md with two-track structure, update PROJECT.md and REQUIREMENTS.md, trim NOTES.md to append-only log, shrink xentient.md to L0 pitch, add SUPERSEDED marker to Phase 2, and update STATE.md.

Purpose: The existing docs still describe the old "n8n-style orchestration" and "visual control plane" vision. This plan rewrites them to reflect the bridge model, ensuring no stale references remain.

Output: Updated ROADMAP.md, PROJECT.md, REQUIREMENTS.md, STATE.md, NOTES.md, xentient.md, SUPERSEDED.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-doc-architecture-refactor/05-CONTEXT.md
@.planning/phases/05-doc-architecture-refactor/05-RESEARCH.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@NOTES.md
@xentient.md
@docs/repurpose.md

<interfaces>
From 05-CONTEXT.md decisions:
- D1: Xentient = IoT terminal (bridge), NOT AI brain
- D17: NONE of Platform Track (P1-P8) ships before Apr 24 demo
- D18: Demo ships current harness as-is
- D20: 4-layer doc system (L0 Pitch, L1 Vision, L2 Spec, L3 Phase, L4 Ops)
- D21: xentient.md shrinks from ~647 to ~200 lines
- D22: NOTES.md becomes append-only decision log
- D23: Phase 2 plans marked SUPERSEDED
- D29: Provider SDK documented as Layer 1, post-demo npm publish
- D30: SDK README included in deliverables
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite ROADMAP.md, update PROJECT.md and REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/05-doc-architecture-refactor/05-CONTEXT.md
    docs/repurpose.md
  </read_first>
  <action>Rewrite three L2 planning documents:

**.planning/ROADMAP.md** — Two-track roadmap (per D17, D18, specific idea 1 from CONTEXT.md):

Replace the current single-track roadmap with a two-track structure:

1. Header: "# Roadmap: Xentient"

2. Section "Overview" — one paragraph: Xentient is the IoT terminal — a thin voice/hardware bridge that lets any AI brain inhabit a physical room. Two tracks: Demo Track (frozen scope, April 24 presentation) and Platform Track (post-capstone evolution, P1-P8).

3. Section "Demo Track (Frozen — Through Apr 24)" with a table:
   | Phase | Goal | Status |
   | 1: Node Base & Comms | ESP32 firmware + MQTT telemetry | Not started |
   | 2: Harness & Intelligence | Voice pipeline + memory (SUPERSEDED by bridge reframe — see below) | Superseded |
   | 3: Web-Control & Assembly | Dashboard + hardware assembly | Not started |
   | 4: Optimization & Demo Prep | Latency + demo narrative | Not started |
   | 5: Doc Architecture Refactor | Restructure docs to bridge model | In progress |

4. Section "Platform Track (Post-Capstone — Apr 25+)" with the P1-P8 table from repurpose.md:
   | Phase | What | LOC Impact | Dependency |
   | P1: Hermes Adapter | Replace custom LLM+memory loop with Hermes connection | -300 LOC, +80 LOC | Hermes installed |
   | P2: Mem0 Integration | Add Mem0 as Hermes plugin + direct API fallback | +30 LOC | Mem0 Docker |
   | P3: Mode Manager | sleep/listen/active/record state machine | +60 LOC | None |
   | P4: Space Manager | Space context + MQTT contract + permissions | +100 LOC | P3 |
   | P5: Pack Loader v2 | New handler types, space awareness | +60 LOC | P1, P4 |
   | P6: Communication Bridge | REST/WS/MQTT bridge to AI brain layer | +100 LOC | P1 |
   | P7: OpenClaw Adapter | Computer-use handler | +60 LOC | P5 |
   | P8: Archon Adapter | Basic YAML DAG workflow delegation | +50 LOC | P5 |

5. Section "Phase Details" — keep Phase 1, 3, 4, 5 details from current ROADMAP (update Phase 5 to reflect completed planning). For Phase 2, add a note: "**Phase 2 is SUPERSEDED.** The n8n-style orchestration vision has been replaced by the bridge model (see docs/VISION.md). The custom memory layer (MEM-01/02/03) will be replaced by Hermes+Mem0 integration in Platform Track P1-P2. Phase 2 deliverables that still ship in the demo (voice pipeline, MQTT bridge, basic memory) are carried forward as-is."

6. Section "Progress" — update the table to reflect Phase 5 in progress.

7. Section "Document Architecture" — reference the 4-layer system (L0-L4) from CONTEXT.md D20 with links:
   - L0: README.md (pitch), xentient.md (narrative)
   - L1: docs/VISION.md, docs/NON_GOALS.md
   - L2: docs/CONTRACTS.md, docs/PACKS.md, docs/SPACES.md, docs/HARDWARE.md, docs/INTEGRATIONS/*.md, .planning/*.md
   - L3: .planning/phases/*
   - L4: NOTES.md (decision log), CLAUDE.md, AGENTS.md

**.planning/PROJECT.md** — Update project charter (per D1):

1. Replace "What This Is" paragraph: "Xentient is the IoT terminal — a thin voice/hardware bridge that lets any AI brain inhabit a physical room. The harness is a terminal OS; intelligence plugs in via integration tiers (basic, Hermes+Mem0, OpenClaw, Archon)."

2. Replace "Core Value" from "Modular, zero-friction spatial intelligence through a visual control plane and high-speed AI orchestration" to: "The bridge between physical rooms and any AI brain. Voice, sensors, display — any intelligence can plug in."

3. Replace "Requirements" section:
   - Validated: ARCH-V1, COMMS-V1, MODEL-V1 (keep as-is)
   - Active: Update to reflect bridge model. Remove HARNESS-CORE ("n8n-style visual workflow engine") and MEMORY-HERMES ("Hermes-Agent proactive memory"). Add:
     - VOICE-PIPELINE: STT→LLM→TTS streaming pipeline (current harness, ships in demo)
     - MQTT-BRIDGE: MQTT hardware bridge + VAD (current, ships in demo)
     - LCD-FACE: I2C 16x2 display state machine (B7, ships in demo)
     - HERMES-INTEGRATION: Replace custom memory with Hermes+Mem0 (Platform Track P1-P2)
     - SPACE-MODE-MGR: Mode and Space managers (Platform Track P3-P4)
   - Out of Scope: Add "n8n-style visual orchestration — superseded by bridge model"

4. Replace "Context" paragraph: "Xentient is in V1 demo prep with a hard deadline of April 24, 2026. Post-demo, the architecture shifts from a custom AI brain to a thin terminal that delegates intelligence to best-in-class integrations (Hermes, Mem0, OpenClaw, Archon)."

5. Update "Key Decisions" table:
   - Change "Web App Control Plane" to "Bridge Model — Xentient is the IoT terminal, not the AI brain"
   - Change "Hermes Memory Pattern" to "Mem0 as primary memory layer (P2), Hermes as default brain (P1)"
   - Keep "Cloud LLM Mandatory" as-is
   - Keep "UART Camera Path" as-is
   - Add: "Demo ships current harness as-is — no Platform Track code before Apr 24"

6. Update "Last updated" timestamp to 2026-04-19.

**.planning/REQUIREMENTS.md** — Update requirements (per CONTEXT.md specifics item 6):

1. Keep NODE-01 through NODE-05 as-is (hardware/firmware, still valid).

2. Update HARN-01 from "n8n-style modular processing pipeline" to "Voice pipeline (STT→LLM→TTS) with streaming audio — thin terminal OS, not orchestration engine".

3. Keep HARN-02 (MQTT brokerage) and HARN-04 (audio ingestion) as-is — still valid.

4. Replace HARN-03 "Integration of Cloud LLM APIs" with: "Cloud LLM provider integration for basic mode (direct provider call, no memory, no skills). Post-demo: Hermes integration (Platform Track P1)."

5. Replace MEM-01 "Persistent user profile and context stored in SQLite/FTS5" with: "MEM-01: Delegate memory to Hermes+Mem0 (Platform Track P1-P2). Demo uses current MemoryDB/FactExtractor/MemoryInjector as temporary implementation. Post-demo, custom memory code (memory/ directory) is DELETED and replaced by ~80 LOC HermesAdapter + ~30 LOC Mem0Adapter."

6. Replace MEM-02 "Proactive memory retrieval (LLM reviews history to inject context)" with: "MEM-02: Proactive memory retrieval via Hermes+Mem0 (Platform Track P1-P2). Mem0 provides semantic extraction, recency weighting, entity resolution, and space-scoped tags natively. No custom FactExtractor/MemoryInjector code remains post-demo."

7. Replace MEM-03 "Cross-session identification and greeting based on persistent user model" with: "MEM-03: Cross-session identification via Hermes+Mem0 (Platform Track P1-P2). Mem0 user-scoped tags enable greeting by name and persistent context across sessions. No custom user model code remains post-demo."

8. Add a new section "Platform Track Requirements (Post-Demo)" with entries:
   - PT-01: Hermes Adapter — Replace custom LLM+memory loop with Hermes connection (P1)
   - PT-02: Mem0 Integration — Direct Mem0 API fallback for basic mode (P2)
   - PT-03: Mode Manager — Sleep/listen/active/record state machine (P3)
   - PT-04: Space Manager — Space context + MQTT contract + permissions (P4)
   - PT-05: Pack Loader v2 — New handler types, space awareness (P5)
   - PT-06: Communication Bridge — REST/WS/MQTT bridge to AI brain layer (P6)
   - PT-07: OpenClaw Adapter — Computer-use handler (P7)
   - PT-08: Archon Adapter — Basic YAML DAG workflow delegation (P8)

9. Keep WEB-01, WEB-02, WEB-03 as-is (still valid for demo).

10. Keep "v2 Requirements (Deferred)" section as-is.

11. Update "Out of Scope" table: Add "n8n-style visual orchestration (superseded by bridge model)" row.

12. Update "Traceability" table to include new PT requirements.

13. Update timestamp to 2026-04-19.</action>
  <verify>
    grep -c "Demo Track\|Platform Track\|P1.*P8" .planning/ROADMAP.md && echo "ROADMAP: two-track structure present"
    grep -c "SUPERSEDED\|bridge reframe\|bridge model" .planning/ROADMAP.md && echo "ROADMAP: Phase 2 superseded note present"
    grep -c "IoT terminal\|bridge\|bridge model" .planning/PROJECT.md && echo "PROJECT: bridge model language present"
    grep -c "visual control plane\|n8n-style" .planning/PROJECT.md || echo "PROJECT: old language removed"
    grep -c "MEM-01\|MEM-02\|MEM-03\|Hermes\|Mem0\|delegat" .planning/REQUIREMENTS.md && echo "REQUIREMENTS: memory delegation present"
    grep -c "PT-0[1-8]\|Platform Track" .planning/REQUIREMENTS.md && echo "REQUIREMENTS: PT requirements present"
    grep -c "voice pipeline\|Voice pipeline" .planning/REQUIREMENTS.md && echo "REQUIREMENTS: HARN-01 rephrased"
  </verify>
  <done>ROADMAP.md rewritten with two-track structure (Demo + Platform). PROJECT.md updated with bridge model. REQUIREMENTS.md updated with delegation reqs and PT requirements. Phase 2 superseded note present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trim NOTES.md, shrink xentient.md, add SUPERSEDED marker, update STATE.md</name>
  <files>NOTES.md, xentient.md, .planning/phases/02-harness-intelligence-layer/SUPERSEDED.md, .planning/STATE.md</files>
  <read_first>
    NOTES.md
    xentient.md
    .planning/STATE.md
  </read_first>
  <action>Restructure four files:

**NOTES.md** — Trim to append-only decision log (per D22):

1. Keep the header rule and format: `> **RULE:** All entries are dated and user-approved only.`

2. Keep the Status Snapshot section (updated to current state).

3. Keep ALL dated entries as-is: [2026-04-18] entries for 3D Enclosure Progress, Software Bug, Deep-Plan Seeds, B1-B7 Decisions, Archon/Library decisions, Execution Order.

4. REMOVE the entire "Platform Vision — SDK + Bot Pack Extensibility" section (lines starting with "## [2026-04-18] Platform Vision — SDK + Bot Pack Extensibility" through the end of the glossary). This content now lives in:
   - Layer 1 → docs/VISION.md (integration tiers, bridge model)
   - Layer 2 → docs/PACKS.md (pack folder spec, manifest, lifecycle, guardrails)
   - Layer 2 → docs/SPACES.md (Space model, Mode state machine)
   - Layer 2 → docs/CONTRACTS.md (wire contracts, MQTT topics)
   - Layer 2 → docs/HARDWARE.md (B1-B7 decisions)
   - Layer 2 → docs/INTEGRATIONS/hermes.md, mem0.md, openclaw.md

5. Add a note at the top of the trimmed file (after the rule, before Status Snapshot):
   ```
   > **Note (2026-04-19):** Platform Vision, Pack System, and SDK content has been extracted to L2 spec docs. See docs/VISION.md, docs/PACKS.md, docs/SPACES.md, docs/CONTRACTS.md, docs/HARDWARE.md, and docs/INTEGRATIONS/*.md. This file is now an append-only decision log for hardware/firmware/demo-critical decisions only.
   ```

6. The trimmed NOTES.md should be approximately 200-250 lines (just the header, note, status snapshot, and dated entries through "Execution Order").

**xentient.md** — Shrink from ~647 lines to ~200 lines (per D21):

1. Keep the title and one-line description: "Xentient — A programmable nervous system for intelligent spaces"

2. Keep the "What Xentient Is" paragraph (first 5-6 lines of current intro — the bridge model positioning).

3. Keep the "The Name" section (3 lines).

4. Keep the "Core Principles" section (5 principles — these are timeless).

5. Replace the "Architecture" section with a much shorter version:
   - 4-line ASCII showing: Node Base (bottom) ↔ Harness (middle) ↔ AI Brain Layer (top)
   - One paragraph: "Xentient is built in four layers. The Node Base is dumb hardware. The Harness is a thin terminal OS. The AI Brain Layer (Hermes, Mem0, OpenClaw, Archon) provides intelligence via integration. Basic mode (direct LLM call) always works without any integration."

6. REMOVE the detailed Layer 1-4 descriptions (Node Base details, Peripheral Units, Snap Interface, Typed Slot Architecture, Universal Slot Architecture, Power Architecture, Firmware vs Profile, Node States, Node Roles, Data Modes — all of this is hardware reference material that belongs in HARDWARE.md or xentient.md's full form).

7. Replace with: "See docs/HARDWARE.md for hardware decisions, BOM, and enclosure specs."

8. Keep a shortened "Spaces" section (3-4 lines explaining the concept, pointing to docs/SPACES.md for full spec).

9. Keep a shortened "Integration Tiers" section (3-4 lines explaining basic/hermes+mem0/openclaw/archon, pointing to docs/VISION.md for full architecture).

10. REMOVE: Data Contract section (moved to docs/CONTRACTS.md), Output Streams section, Identity and Privacy section, Hardware Reference section (moved to docs/HARDWARE.md), Lifecycle sections, Roadmap section (moved to ROADMAP.md), What Xentient Is Not section (moved to docs/NON_GOALS.md), Taglines section.

11. Add a "Full Documentation" section at the bottom linking to:
   - docs/VISION.md — Full architecture and integration tiers
   - docs/CONTRACTS.md — Wire format specification
   - docs/PACKS.md — Pack system specification
   - docs/SPACES.md — Space model and Mode state machine
   - docs/HARDWARE.md — Locked hardware decisions (B1-B7)
   - docs/NON_GOALS.md — What Xentient is NOT
   - docs/INTEGRATIONS/ — Integration adapter contracts

12. Target: ~200 lines total. Punchy. Narrative pitch only (L0).

**.planning/phases/02-harness-intelligence-layer/SUPERSEDED.md** (per D23):

Create a new file in the Phase 2 directory:
```markdown
# SUPERSEDED — Bridge Model Reframe

**Date:** 2026-04-19
**Reason:** The "n8n-style orchestration" vision for Phase 2 has been superseded by the bridge model (Xentient = IoT terminal, not AI brain). The custom memory layer (MemoryDB/FactExtractor/MemoryInjector) will be replaced by Hermes+Mem0 integration in Platform Track P1-P2.

**What changed:**
- Phase 2 was originally scoped as "Build the n8n-style orchestration engine and Hermes-Agent memory"
- The bridge reframe means Xentient no longer builds the AI brain — it delegates to Hermes/Mem0/OpenClaw/Archon
- The voice pipeline (STT→LLM→TTS), MQTT bridge, and basic memory still ship in the demo
- Custom memory code (memory/ directory) will be deleted post-demo and replaced by thin adapters

**What still ships in the demo:**
- Voice pipeline (STT→LLM→TTS streaming)
- MQTT hardware bridge + VAD
- Basic memory (MemoryDB/FactExtractor/MemoryInjector — temporary, replaced in P1-P2)
- LCD display (B7)

**What moved to Platform Track:**
- P1: Hermes Adapter (replaces custom LLM+memory loop)
- P2: Mem0 Integration (replaces MemoryDB/FactExtractor/MemoryInjector)
- P3-P8: Mode Manager, Space Manager, Pack Loader v2, Communication Bridge, OpenClaw, Archon

**Reference:** See docs/VISION.md for the full bridge model architecture and migration path.
```

**.planning/STATE.md** — Update project state:

1. Update `last_updated` to 2026-04-19.
2. Update `status` to `in-progress`.
3. Update `progress.total_phases` to 5.
4. Update `current focus` to "Phase 5 — doc-architecture-refactor".
5. Update "Recent Decisions" section to include the bridge model reframe:
   - "Bridge model adopted: Xentient = IoT terminal, not AI brain"
   - "4-layer doc system established (L0-L4)"
   - "Phase 2 plans superseded by bridge reframe"
   - "Two-track roadmap: Demo Track (frozen) + Platform Track (P1-P8)"
6. Update milestone status to reflect Phase 5 in progress.</action>
  <verify>
    grep -c "append-only\|L2 spec\|extracted to" NOTES.md && echo "NOTES: extraction note present"
    test $(wc -l < NOTES.md) -le 250 && echo "NOTES: trimmed to reasonable size"
    grep -c "IoT terminal\|bridge\|docs/VISION\|docs/HARDWARE" xentient.md && echo "XENTIENT: bridge model and doc links present"
    test $(wc -l < xentient.md) -le 220 && echo "XENTIENT: shrunk to ~200 lines"
    grep -c "SUPERSEDED\|bridge reframe\|bridge model" .planning/phases/02-harness-intelligence-layer/SUPERSEDED.md && echo "SUPERSEDED: marker present"
    grep -c "Phase 5\|doc-architecture\|bridge model" .planning/STATE.md && echo "STATE: updated for Phase 5"
    ! grep -c "Platform Vision.*SDK\|Bot Pack Extensibility" NOTES.md && echo "NOTES: platform vision section removed"
  </verify>
  <done>NOTES.md trimmed to append-only decision log with platform content extracted to L2 specs. xentient.md shrunk to ~200-line L0 narrative pitch. SUPERSEDED.md added to Phase 2 directory. STATE.md updated for Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → implementation | Stale planning docs could mislead developers into building old architecture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | ROADMAP.md, PROJECT.md, REQUIREMENTS.md | mitigate | All old "n8n-style" and "visual control plane" language replaced with bridge model language; SUPERSEDED marker prevents confusion |
| T-05-06 | Repudiation | NOTES.md trim | mitigate | Extraction note in NOTES.md references exactly which docs received the extracted content |
</threat_model>

<verification>
1. ROADMAP.md shows two-track structure (Demo Track + Platform Track P1-P8)
2. PROJECT.md reflects bridge model, not "visual control plane"
3. REQUIREMENTS.md has MEM-01/02/03 replaced with delegation reqs, HARN-01 rephrased
4. NOTES.md is trimmed to append-only decision log (~200-250 lines)
5. xentient.md shrunk to ~200 lines as L0 pitch
6. SUPERSEDED.md exists in Phase 2 directory
7. STATE.md reflects Phase 5 as current focus
8. No stale "n8n-style orchestration" or "visual control plane" language remains in any modified file
</verification>

<success_criteria>
- ROADMAP.md has two tracks: Demo Track (frozen, through Apr 24) and Platform Track (P1-P8, post-capstone)
- PROJECT.md core value and description reflect bridge model
- REQUIREMENTS.md has updated MEM/HARN requirements and new PT-01 through PT-08
- NOTES.md trimmed to dated entries only, platform content extracted
- xentient.md is ~200 lines with links to full docs
- Phase 2 has SUPERSEDED.md explaining the bridge reframe
- STATE.md reflects Phase 5 in progress
- All docs committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-doc-architecture-refactor/05-03-SUMMARY.md`
</output>