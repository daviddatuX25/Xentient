---
phase: 05-doc-architecture-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/VISION.md
  - docs/NON_GOALS.md
  - docs/HARDWARE.md
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-06
  - DOC-07

must_haves:
  truths:
    - "VISION.md contains the bridge model (Xentient = IoT terminal, not AI brain), integration tiers (basic/hermes+mem0/openclaw/archon), and Spaces/Modes primitives"
    - "NON_GOALS.md explicitly lists what Xentient is NOT, including no Archon in v1 demo"
    - "HARDWARE.md contains B1-B7 decisions verbatim from NOTES.md"
    - "README.md reads as a 60-second pitch aligned with the bridge model"
    - "Archon contradiction is resolved: included as P9 in Platform Track, explicitly deferred in demo scope"
    - "Three-tier architecture explicit: Hardware / Core (Runtime + Web Control Panel) / AI Brain (Hermes, Mem0, OpenClaw, Archon)"
    - "'Tool Router' renamed to 'Brain Router' — it is the routing layer between Core and the AI Brain tier"
  artifacts:
    - path: "docs/VISION.md"
      provides: "L1 vision document with bridge model, integration tiers, architecture diagram, Spaces/Modes"
      min_lines: 80
    - path: "docs/NON_GOALS.md"
      provides: "Explicit exclusion list for v1 and Platform Track"
      min_lines: 30
    - path: "docs/HARDWARE.md"
      provides: "Locked B1-B7 decisions, BOM, enclosures, LCD spec"
      min_lines: 60
    - path: "README.md"
      provides: "60-second pitch with bridge model framing"
      min_lines: 20
  key_links:
    - from: "docs/VISION.md"
      to: "docs/repurpose.md"
      via: "content derivation"
      pattern: "bridge model|IoT terminal|integration tiers"
    - from: "docs/HARDWARE.md"
      to: "NOTES.md"
      via: "verbatim B1-B7 extraction"
      pattern: "B1\\.|B2\\.|B3\\.|B4\\.|B5\\.|B6\\.|B7\\."
---

<objective>
Create the four foundational L0/L1 documents that anchor the entire documentation restructure. VISION.md promotes repurpose.md into the canonical vision. NON_GOALS.md draws hard boundaries. HARDWARE.md preserves all locked decisions verbatim. README.md becomes the 60-second pitch.

Purpose: These four docs are the source of truth that all other L2 specs will reference. They must be written first because CONTRACTS, PACKS, SPACES, and INTEGRATIONS all derive from them.

Output: docs/VISION.md, docs/NON_GOALS.md, docs/HARDWARE.md, README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-doc-architecture-refactor/05-CONTEXT.md
@.planning/phases/05-doc-architecture-refactor/05-RESEARCH.md
@docs/repurpose.md
@NOTES.md
@README.md
@xentient.md

<interfaces>
From NOTES.md (source of truth for B1-B7):
- B1: Harness Retry Bug — auto-retry max 3, exponential backoff, p-retry library, state machine
- B2: Shared Data Contract — Zod schemas, camelCase, epoch-millis uint32, v:1 field, 3KB cap, peripheral ID registry
- B3: Node Base Firmware — 16kHz mono PCM S16LE, raw PCM no Opus, DMA ring buffer, FreeRTOS priorities, ArduinoWebsockets + PubSubClient + ArduinoJson v7
- B4: EEPROM Enumeration DROPPED — compile-time peripheral map in firmware/config/peripherals.h
- B5: JST Connectors — 4-pin (I2C) and 6-pin (I2S), pigtail wiring, heatshrink color code
- B6: Enclosures — PETG, slot-in slide mount, design peripherals first then dock, 45deg max overhangs
- B7: LCD Display — I2C 16x2 PCF8574 at 0x27, core to Node Base dock, dual-mode expressive + functional, LiquidCrystal_I2C library

From repurpose.md (source for VISION.md):
- Bridge model architecture diagram
- Integration tiers: basic/hermes+mem0/openclaw/archon
- Spaces model with SpaceMode state machine
- Tool Router handler table (8 handler types)
- What Xentient owns vs delegates table
- Post-demo migration path P1-P8
- Demo day narrative (ships current harness as-is)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create VISION.md from repurpose.md</name>
  <files>docs/VISION.md</files>
  <read_first>
    docs/repurpose.md
    .planning/phases/05-doc-architecture-refactor/05-CONTEXT.md
  </read_first>
  <action>Promote docs/repurpose.md into docs/VISION.md as the canonical L1 vision document. This is NOT a copy — it is a curated promotion with the following changes:

1. Remove the "Date / Status / Scope / Author" header preamble — VISION.md is evergreen, not dated.

2. Keep the "The Reframe" section exactly as-is (per D1).

3. Keep the "What Xentient Owns vs What It Delegates" table (per D1, D2).

4. Keep the "Integration Flexibility Principle" section with the 4 integration tiers (per D2):
   - basic (direct LLM, no memory)
   - hermes+mem0 (default)
   - hermes+mem0+openclaw (computer use)
   - hermes+mem0+openclaw+archon (coding workflows)

5. Keep the "Architecture: The Bridge Model" section with the full ASCII architecture diagram. Update the diagram to show the three-tier model explicitly: **Tier 1 Hardware** (Node Bases + peripherals) ↔ **Tier 2 Core** (always-on hosted: Runtime daemon + Web Control Panel, both sharing the same codebase) ↔ **Tier 3 AI Brain** (remote/sandboxed: Hermes, Mem0, OpenClaw, Archon). Add a paragraph noting the Core runs live 24/7 because it owns hardware state; Web Control Panel is Face B of the same Core, providing hardware config, pack/space/permission management, and integration toggles.

6. Keep the "Spaces: Identity, Permissions, Modes" section with Space interface, SpaceMode type, mode transition diagram, and Space examples table (per D3, D4).

7. Keep the "Integration Details" section (Hermes, Mem0, OpenClaw, Archon).

8. Rename the "Tool Router" section to "Brain Router" with handler table (per D5). Brain Router = the routing layer inside Core that dispatches handler invocations across the three tiers (Hardware via MQTT, Core-local basic LLM, or AI Brain tier via Hermes/OpenClaw/Archon adapters).

9. Keep the "Communication Layer: Server Architecture" section (per D6).

10. Keep the "Concrete Demo: Daily Prayer Workflow" section.

11. Keep the "Post-Demo Migration Path" section but update the table to P1-P9 (per D10, D17). Order: P1 Hermes Adapter, P2 Mem0 Integration, P3 Mode Manager, P4 Space Manager, P5 Pack Loader v2, P6 Web Control Panel (NEW — Core's second face: hardware config UI, sleep state control, pack/space/permission management, integration toggles, live telemetry), P7 Communication Bridge (REST/WS/MQTT bridge between Core and AI Brain tier), P8 OpenClaw Adapter, P9 Archon Adapter.

12. Keep the "Glossary" section.

13. KEEP the "Demo Day (Apr 24) — What Ships" section verbatim — it is critical context.

14. KEEP the "Preserved Context: Pre-Reframe Decisions" section — hardware decisions must be referenceable.

15. REMOVE the "Open Questions" section — those belong in individual spec docs, not the vision.

16. ADD a document header: "# Xentient Vision\n\n> The authoritative vision document. Describes what Xentient is, what it owns, what it delegates, and how it evolves post-demo."

17. ADD a "Document Layer" note at the top: "L1 Vision — Defines the bridge model and integration architecture. Referenced by all L2 spec docs."

The result should be a clean, authoritative vision document that reads top-to-bottom without redundancy.</action>
  <verify>
    grep -c "bridge model\|IoT terminal" docs/VISION.md && echo "VISION: bridge model present"
    grep -c "integration tier\|basic.*hermes\|hermes+mem0" docs/VISION.md && echo "VISION: tiers present"
    grep -c "Space\b.*Mode\|SpaceMode\|sleep.*listen.*active.*record" docs/VISION.md && echo "VISION: Spaces/Modes present"
    grep -c "P1.*P8\|Hermes Adapter\|Post-Demo Migration" docs/VISION.md && echo "VISION: migration path present"
    grep -c "Archon" docs/VISION.md && echo "VISION: Archon included (not omitted)"
    test $(wc -l < docs/VISION.md) -ge 80 && echo "VISION: minimum line count met"
  </verify>
  <done>VISION.md exists as the canonical L1 vision document with bridge model, integration tiers, Spaces/Modes primitives, Tool Router, and P1-P8 migration path. Archon is included as P8.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create NON_GOALS.md and HARDWARE.md, rewrite README.md</name>
  <files>docs/NON_GOALS.md, docs/HARDWARE.md, README.md</files>
  <read_first>
    NOTES.md
    .planning/phases/05-doc-architecture-refactor/05-CONTEXT.md
    docs/repurpose.md
    README.md
    xentient.md
  </read_first>
  <action>Create three documents:

**docs/NON_GOALS.md** — Explicit "what Xentient is NOT" list (per D20, D28):

1. Header: "# Xentient Non-Goals\n\n> What Xentient explicitly does NOT do. This list prevents scope creep."

2. Section "v1 Demo Non-Goals (through Apr 24)":
   - No Hermes/Mem0/OpenClaw/Archon integration code ships before demo (per D17)
   - No Pack system, no Space Manager, no Mode Manager (per D17)
   - No Communication Bridge code
   - No Provider SDK npm publish (per D29 — post-demo)
   - No Visual Builder / web UI (per CONTEXT.md deferred ideas)

3. Section "Platform Non-Goals (v1 scope)":
   - No custom DSL — markdown prompts + JSON tools cover 95% of use cases (per D28)
   - No sandboxing / VM isolation — handlers are enum-gated, no eval, no dynamic require (per D28)
   - No versioned pack migrations for v1 — manifest version mismatch = load fails with clear error (per D28)
   - No pack dependencies — packs are flat, self-contained (per D28)
   - No remote pack fetch at boot — local folders only (per D28)
   - No multi-pack active state — one pack at a time (per D28)
   - No Archon full coding agent — P8 basic YAML DAG workflows only (per D10, D11)

4. Section "Out of Scope (future consideration, not v1 or Platform Track P1-P8)":
   - Visual builder / web UI (Xentient-a8u retarget) — future phase (per CONTEXT.md deferred)
   - Pack marketplace / gallery — post-v1
   - Remote pack fetch — v1 NON_GOAL
   - Multi-pack active state — v1 NON_GOAL
   - Custom DSL for behavior scripting
   - Universal slot architecture (v2 hardware target)
   - On-device speech-to-text (STT offloaded for v1 accuracy)

**docs/HARDWARE.md** — Locked B1-B7 decisions, BOM, enclosures, LCD (per D12-D16, D27):

1. Header: "# Xentient Hardware Decisions\n\n> Locked hardware and firmware decisions. Source of truth for B1-B7. These decisions are NOT negotiable — they are the hardware foundation that software builds upon."

2. Section "Decision Lock Table" with each B1-B7 as a subsection:
   - **B1: Harness Retry Bug** — Copy verbatim from NOTES.md "B1. Retry Bug — DECIDED" section: auto-retry max 3, exponential backoff (1s/2s/4s), transient vs fatal distinction, UI fallback, state machine, p-retry library.
   - **B2: Shared Data Contract** — Copy verbatim from NOTES.md "B2. Data Contract — DECIDED" section: source of truth in contracts.ts, Zod schemas, camelCase, epoch-millis uint32, v:1 field, 3KB cap, peripheral ID registry.
   - **B3: Node Base Firmware** — Copy verbatim from NOTES.md "B3. Node Base Firmware — DECIDED" section: 16kHz mono PCM S16LE, raw PCM no Opus, DMA ring buffer, task priorities, ArduinoWebsockets + PubSubClient + ArduinoJson v7.
   - **B4: EEPROM Enumeration — DROPPED** — Copy verbatim from NOTES.md "B4. EEPROM Enumeration — DROPPED" section: compile-time peripheral map, type registry.
   - **B5: JST Connectors** — Copy verbatim from NOTES.md "B5. JST Connectors — DECIDED" section: 4-pin and 6-pin pinouts, pigtail wiring, heatshrink color code.
   - **B6: Enclosures** — Copy verbatim from NOTES.md "B6. Enclosures — DECIDED" section: PETG, slot-in slide mount, design peripherals first then dock.
   - **B7: LCD Display** — Copy verbatim from NOTES.md "B7. LCD Display (NEW) — DECIDED" section: I2C 16x2 at 0x27, dual-mode, display_update contract, LiquidCrystal_I2C library.

3. Section "Bill of Materials" — list the known BOM items from xentient.md: ESP32-WROOM-32, W5500, INMP441, MAX98357A, OV2640, HC-SR501, BME280, I2C 16x2 LCD (PCF8574), TP4056, MT3608, JST-SH connectors.

4. Section "Enclosure Specifications" — PETG, 45deg max overhangs, M2 heat inserts for dock, press-fit for peripherals.

5. Section "Sample Rate & Audio Format" — 16kHz mono PCM S16LE, raw, no on-device compression.

6. Note at bottom: "These decisions are sourced verbatim from NOTES.md (2026-04-18) and are the authoritative reference for all hardware and firmware work."

**README.md** — Rewrite as a 60-second pitch aligned with bridge model (per D1, D20):

1. Title: "# Xentient"
2. One-line description: "The IoT terminal that gives any AI a physical presence in any room."
3. Subtitle/tagline: "Voice. Sensors. Display. Any AI brain can plug in."
4. Section "What It Does" — 3-4 sentence bridge model explanation: Xentient is the bridge between physical rooms and AI brains. The harness is a thin terminal OS. Voice pipeline, MQTT bridge, LCD faces, mode management — that is what Xentient uniquely owns. Intelligence plugs in via Hermes, Mem0, OpenClaw, or basic LLM.
5. Section "Demo Status" — brief note: V1 prototype demo Apr 24, 2026. Voice pipeline + MQTT bridge + LCD + basic memory.
6. Section "Quick Start" — link to docs/VISION.md for full architecture and docs/HARDWARE.md for hardware decisions.
7. Section "Architecture" — 5-line ASCII showing: Node Base (bottom) ↔ Harness (middle) ↔ AI Brain Layer (top). No more than 10 lines total.
8. Section "Documentation" — table linking to VISION.md, HARDWARE.md, NON_GOALS.md, CONTRACTS.md, PACKS.md, SPACES.md.
9. Remove all the old "Interactive Showcase", "Node Builder", "Flow Simulator" language from the current README.

The README should be short (40-60 lines max). Punchy. Bridge-model aligned. No emoji. No marketing fluff.</action>
  <verify>
    grep -c "NOT.*do\|NOT.*build\|No custom DSL\|No sandboxing\|No Archon" docs/NON_GOALS.md && echo "NON_GOALS: explicit exclusions present"
    grep -c "demo.*Apr.*24\|v1.*demo\|Platform Track" docs/NON_GOALS.md && echo "NON_GOALS: demo scope boundaries present"
    grep -c "B1\.\|B2\.\|B3\.\|B4\.\|B5\.\|B6\.\|B7\." docs/HARDWARE.md && echo "HARDWARE: all B1-B7 present"
    grep -c "16kHz mono PCM\|compile-time peripheral\|PETG\|I2C 16x2\|0x27" docs/HARDWARE.md && echo "HARDWARE: key specs present"
    grep -c "IoT terminal\|bridge" README.md && echo "README: bridge model language present"
    grep -c "VISION.md\|HARDWARE.md\|NON_GOALS.md" README.md && echo "README: doc links present"
    test $(wc -l < docs/NON_GOALS.md) -ge 30 && echo "NON_GOALS: minimum line count met"
    test $(wc -l < docs/HARDWARE.md) -ge 60 && echo "HARDWARE: minimum line count met"
  </verify>
  <done>NON_GOALS.md lists all explicit exclusions for v1 demo and Platform Track. HARDWARE.md preserves B1-B7 verbatim. README.md is a 60-second pitch aligned with the bridge model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation reader → project | No trust boundary — docs are public project artifacts, not runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | HARDWARE.md | accept | Static document — version control (git) provides audit trail for changes |
| T-05-02 | Tampering | NON_GOALS.md | mitigate | Scope boundaries are explicit and verifiable; any addition must be tracked in CONTEXT.md decisions |
</threat_model>

<verification>
1. docs/VISION.md exists and contains bridge model, integration tiers, Spaces/Modes
2. docs/NON_GOALS.md exists and lists all explicit exclusions
3. docs/HARDWARE.md exists and contains B1-B7 verbatim
4. README.md rewritten as bridge-model pitch under 60 lines
5. Archon contradiction resolved: P8 in VISION.md, excluded from demo in NON_GOALS.md
</verification>

<success_criteria>
- VISION.md promoted from repurpose.md with bridge model, integration tiers, Spaces/Modes, P1-P8
- NON_GOALS.md explicit about no Archon in v1, no custom DSL, no sandboxing, no pack dependencies
- HARDWARE.md preserves B1-B7 decisions word-for-word from NOTES.md
- README.md is a concise bridge-model pitch, no stale "n8n" or "visual control plane" language
- All 4 files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-doc-architecture-refactor/05-01-SUMMARY.md`
</output>