---
context: phase
phase: 06-xentient-layers
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-04-27
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

- [x] CONSTRAINT: tsc-oom — TypeScript compiler runs out of memory on default heap. Must use `NODE_OPTIONS="--max-old-space-size=8192"` when running `npx tsc --noEmit`. Standard `npx tsc` will crash with heap OOM.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| tsc-oom | `npx tsc --noEmit` crashes with heap OOM on default Node memory limit. Project has too many type roots for default 1.5GB heap. | blocking | Always run `NODE_OPTIONS="--max-old-space-size=8192" npx tsc --noEmit` to verify compilation |

<current_state>
Phase 6 (Xentient Layers) execution in progress. Waves 1 and 2 are COMPLETE. TypeScript compiles clean. Wave 3 (SpaceManager + MCP Skill Tools) is next.
</current_state>

<completed_work>

Completed Tasks:
- Wave 1 (06-01): CoreSkill & Mode Type System — DONE
  - Added CoreSkill, Space, SkillTrigger, CoreAction, EscalationConfig, ObservabilityEvent, SkillLogEntry, ConflictResolution, PendingConflict types to `harness/src/shared/types.ts`
  - Added SKILL_EVENTS, SKILL_TOOLS, BUILTIN_SKILL_IDS, SKILL_LOG_CAPACITY, CONFLICT_TIMEOUT_MS constants to `harness/src/shared/contracts.ts`
  - TypeScript compiles clean

- Wave 2 (06-02): SkillExecutor — DONE
  - Created `harness/src/engine/SkillLog.ts` — ring buffer skill execution log
  - Created `harness/src/engine/contextBuilders.ts` — escalation context builder with 4 modes (minimal, sensor-snapshot, camera-snapshot, full-context)
  - Created `harness/src/engine/builtins.ts` — 4 builtin CoreSkills (_pir-wake, _idle-sleep, _sensor-telemetry, _determine-skill)
  - Created `harness/src/engine/SkillExecutor.ts` — full tick loop, L1 action execution, escalation pipeline, conflict handling, event/cron/interval scheduling
  - Installed `node-cron` and `@types/node-cron` packages
  - TypeScript compiles clean (requires 8GB heap)
</completed_work>

<remaining_work>

- Wave 3 (06-03): SpaceManager + MCP Skill Tools — NOT STARTED
  - Create SpaceManager class
  - Add 8 MCP skill management tools to mcp/tools.ts
  - Add skill observability events to mcp/events.ts

- Wave 4 (06-04): Wire SpaceManager into Core — NOT STARTED
  - Update core.ts to instantiate SpaceManager
  - Create default Space on startup
  - Forward MQTT events to skill execution
  - Pipe observability events to ControlServer SSE stream

- Wave 5 (06-05): Tests — NOT STARTED
  - Vitest unit tests for SkillExecutor, SkillLog, SpaceManager
</remaining_work>

<decisions_made>

- Used `node-cron` for cron-based skill triggers (per plan spec)
- SkillExecutor extends EventEmitter for LCD/chime action emission
- MqttClient.publish() used directly (not sendCommand which doesn't exist)
- Used mcpServer.notification() (high-level Promise-returning API) instead of mcpServer.server.notification() (low-level raw method)
- SkillTrigger uses discriminated union type pattern for type safety
- SpaceMode is aliased from contracts.ts Mode type to prevent confusion with BehavioralMode
</decisions_made>

<blockers>
- None currently
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-xentient-layers/06-CONTEXT.md` — Full phase context, decisions, and canonical refs
2. `.planning/phases/06-xentient-layers/06-03-PLAN.md` — Next plan to execute (SpaceManager + MCP tools)
3. `docs/SPEC-xentient-layers.md` — Primary spec (THE LAW for this phase)
4. `docs/SPEC-heartbeat-rule-engine.md` — Foundation spec (RuleEngine design)
5. `harness/src/engine/SkillExecutor.ts` — Just created, needed for SpaceManager wiring

## Critical Anti-Patterns (do NOT repeat these)
- tsc OOM: Always use `NODE_OPTIONS="--max-old-space-size=8192"` for TypeScript compilation checks

## Infrastructure State
- node-cron + @types/node-cron: installed
- tsc verification: requires 8GB heap

## Pre-Execution Critique Required
- N/A — execution is in progress, not between design and execution

<context>
Phase 6 execution using /gsd-execute-phase workflow. Waves 1-2 complete and verified. TypeScript compiles clean. The key insight is that this project has a large enough type graph that `npx tsc --noEmit` crashes with default heap, so always use `NODE_OPTIONS="--max-old-space-size=8192"`.

The SkillExecutor is the core engine — it handles tick-based evaluation, L1 action execution, escalation to Brain via MCP notifications, and conflict resolution with timeout fallback. The next step is Wave 3 which creates SpaceManager (multi-space orchestration) and the 8 MCP skill management tools.
</context>

<next_action>
Start with: Execute Wave 3 — read `.planning/phases/06-xentient-layers/06-03-PLAN.md` and create `harness/src/engine/SpaceManager.ts`, then modify `harness/src/mcp/tools.ts` and `harness/src/mcp/events.ts`.
</next_action>