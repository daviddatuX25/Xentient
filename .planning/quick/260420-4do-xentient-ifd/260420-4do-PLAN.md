---
mode: quick
description: Mode Manager wired into Core — integrate ModeManager into index.ts and Pipeline, add MQTT control subscriptions, idle timeouts, and PIR wake transitions
quick_id: 260420-4do
slug: xentient-ifd
date: 2026-04-20
depends_on:
  - 260419-abs (PlatformIO firmware scaffold)
  - 260420-cg9 (MQTT pub/sub client)
---

# Quick Task 260420-4do: Mode Manager (wired into Core)

## Context

The `ModeManager` class exists at `harness/src/engine/ModeManager.ts` but is **not wired into the Core runtime**. It's an orphan — never instantiated in `index.ts`, never connected to MQTT events, never integrated with the Pipeline. This task connects it end-to-end.

### Dependencies (COMPLETE)
- `Xentient-abs` — PlatformIO firmware scaffold (done)
- `Xentient-cg9` — MQTT pub/sub client with auto-reconnect (done)

### What exists
- `ModeManager.ts` — state machine with `transition()`, `forceSet()`, `getMode()`, publishes `xentient/status/mode`
- `MqttClient.ts` — already subscribes to `xentient/control/mode`, emits `modeCommand` event
- `Pipeline.ts` — audio processing pipeline (STT → LLM → TTS), no mode awareness
- `contracts.ts` — `MODE_TRANSITIONS`, `Mode` type, `ModeSet`/`ModeStatus` Zod schemas
- Firmware `mqtt_client.cpp` — subscribes to `xentient/control/mode`, logs payload (no mode handling)

### What's missing
1. ModeManager instantiation in `index.ts`
2. MQTT `modeCommand` → ModeManager.transition() wiring
3. MQTT `sensor` PIR event → ModeManager transition (sleep→listen)
4. Pipeline mode awareness (drop audio in sleep, process in active, etc.)
5. Idle timeouts (listen→sleep after 60s, active→listen after 5min)
6. LCD display commands on mode transitions (publish `xentient/display`)

---

## Tasks

### Task 1: Wire ModeManager into index.ts and connect MQTT events
- **action**: In `harness/src/index.ts`, import and instantiate `ModeManager`, wire `mqtt.on('modeCommand')` to call `modeManager.transition()`, wire `mqtt.on('sensor')` PIR events to trigger sleep→listen, start idle timers
- **files**:
  - `harness/src/index.ts`
  - `harness/src/engine/ModeManager.ts` (add idle timeout logic, PIR wake, LCD display publish)
- **verify**: `index.ts` instantiates ModeManager, subscribes to modeCommand and PIR events; ModeManager publishes display updates on transitions
- **done**: `bun run --dry-run` (or typecheck) passes; ModeManager is connected to MQTT lifecycle

### Task 2: Make Pipeline mode-aware
- **action**: Add `modeManager` reference to Pipeline; in `audioChunk` handler, check mode — drop audio when mode is `sleep`; in VAD handlers, only process when mode is `active`; pause STT/TTS when mode is `listen` (buffer only); record mode when `record`
- **files**:
  - `harness/src/engine/Pipeline.ts`
  - `harness/src/engine/ModeManager.ts` (if emitting mode change events for Pipeline)
- **verify**: Pipeline checks current mode before processing audio; audio is dropped in sleep mode; full pipeline runs in active mode
- **done**: Pipeline respects mode state; mode transitions correctly gate audio processing

### Task 3: Add idle timeouts and publish display updates on transitions
- **action**: In ModeManager, implement idle timeouts per SPACES.md (listen→sleep after 60s, active→listen after 5min = 300s); publish `xentient/display` messages on each transition showing the appropriate LCD face; emit Node.js events for Pipeline to react to
- **files**:
  - `harness/src/engine/ModeManager.ts`
  - `harness/src/shared/contracts.ts` (add LCD face constants if needed)
- **verify**: ModeManager auto-transitions after idle periods; LCD display commands sent on mode changes; events emitted for Pipeline
- **done**: Idle timeouts work; LCD face updates publish on `xentient/display`; mode change events propagate