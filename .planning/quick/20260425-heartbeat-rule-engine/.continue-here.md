---
context: phase
phase: heartbeat-rule-engine
task: Phase 0 complete, Phase 2 starting
total_tasks: 11 (across Phases 0-3)
status: in_progress
last_updated: 2026-04-25
---

## BLOCKING CONSTRAINTS

- [ ] CONSTRAINT: MCP SDK transport — Phase 3 should use `StreamableHTTPServerTransport` (not legacy `SSEServerTransport`) per MCP 2025-03-26 spec. Import path: `@modelcontextprotocol/sdk/server/streamableHttp.js`. Verified at SDK v1.29.0.
- [ ] CONSTRAINT: `node-cron` package not yet installed — needed for Phase 2 cron triggers. Install before T3.
- [ ] CONSTRAINT: `express` package not yet installed — needed for Phase 3 SSE transport. Install before T14.

<current_state>
Executing Heartbeat & Rule Engine plan (`docs/PLAN-heartbeat-rule-engine.md`) using subagent-driven development in worktree `heartbeat-rule-engine`. Phase 0 complete. Phase 1 harness fixes partially done (HW-1 was already done in prior commit, HW-2 already exists in AudioServer.ts, HW-3 completed — LCD_FACES updated + PIPELINE_FACES added). Phase 2 (Rule Engine) tasks T1-T11 next.
</current_state>

<completed_work>

Phase 0 — Pre-flight (ALL COMPLETE):
- P0-1: Chime WAV files generated (morning.wav, alert.wav, chime.wav) in `harness/assets/chimes/`. Script at `harness/scripts/generate-chimes.js`.
- P0-2: MCP SDK SSE import verified. All 3 transport paths work at SDK v1.29.0. Documentation comment added to `harness/src/mcp/server.ts`.
- P0-3: Rules config added to `harness/config/default.json` (3 static rules: pir-wake, high-temp-warning, morning-chime).

Phase 1 — Harness Fixes (PARTIAL):
- HW-1: Already done (commit f332763 removed dead VAD subscription)
- HW-2: Already done (AudioServer.sendAudio() already prefixes with 0xA0)
- HW-3: DONE — Updated LCD_FACES in `harness/src/shared/contracts.ts` to align with HARDWARE.md spec. Added PIPELINE_FACES mapping for pipeline-state-driven faces.

Firmware tasks (FW-1 through FW-4) NOT done — require hardware flashing.
</completed_work>

<remaining_work>

Phase 2 — Rule Engine (T1-T9, NOT STARTED):
- T1: Add Rule types to `src/shared/types.ts` (Rule, Trigger, Condition, RuleAction, RuleContext, BrainHealth)
- T2: Add Zod schemas to `src/shared/contracts-schemas.ts` (RuleTriggerSchema, RuleActionSchema, etc.)
- T3: Create `src/engine/RuleEngine.ts` (needs `node-cron` installed first)
- T4: Unit tests for RuleEngine
- T5: Wire RuleEngine into `src/core.ts` + implement executeFastAction/sendRuleTriggeredNotification
- T6: Add `xentient/rule_triggered` to `src/mcp/events.ts` and MCP_EVENTS
- T7: Add `xentient_register_rule` to `src/mcp/tools.ts`
- T8: Add `xentient_unregister_rule` to `src/mcp/tools.ts`
- T9: Add `xentient_list_rules` to `src/mcp/tools.ts`

Phase 3 — Health Monitor & SSE (T10-T15, NOT STARTED):
- T10-T13: HealthMonitor + brain_status events
- T14-T15: SSE transport + config

Phase 4 — Hermes Integration (NOT STARTED)
</remaining_work>

<decisions_made>

- AudioServer.sendAudio() already has 0xA0 prefix — HW-2 is DONE, no change needed
- MCP SDK v1.29.0 has 3 transport options; StreamableHTTPServerTransport recommended over legacy SSE for Phase 3
- PIPELINE_FACES added alongside LCD_FACES for pipeline-state-driven display (separate from mode-based faces)
- LCD_FACES updated: sleep/record line2 now shows "  ready..." / empty, listen shows "listening...", active shows "(^_^) Xentient" / "  ready..."
- Firmware tasks (FW-1 through FW-4) deferred to hardware session — cannot flash ESP32 from this environment
</decisions_made>

<blockers>
- Firmware tasks require physical ESP32 hardware — cannot be done in this worktree
- TypeScript compilation not yet verified after all changes
- No git commit made yet for Phase 0/1 changes
</blockers>

## Required Reading
1. `docs/PLAN-heartbeat-rule-engine.md` — Full implementation plan with all tasks
2. `docs/SPEC-heartbeat-rule-engine.md` — Detailed specification for rule engine types and behavior
3. `harness/src/shared/contracts.ts` — Updated with PIPELINE_FACES and modified LCD_FACES

## Infrastructure State
- Worktree: `D:\Projects\Xentient\.claude\worktrees\heartbeat-rule-engine` on branch `worktree-heartbeat-rule-engine`
- Harness TypeScript project: `harness/` directory
- Node modules installed in worktree
- No running servers

<context>
Was executing subagent-driven-development workflow. Phase 0 dispatched 3 parallel subagents (all completed successfully). Phase 1 harness fixes done (HW-1/2 were pre-existing, HW-3 completed inline). Was about to start Phase 2 Rule Engine implementation when user paused. The plan is clear and tasks are well-defined — next step is T1 (add Rule types).
</context>

<next_action>
Start with T1: Add Rule, Trigger, Condition, RuleAction, RuleContext, and BrainHealth types to `harness/src/shared/types.ts`. Then T2: Add Zod schemas. Then install `node-cron @types/node-cron` before T3.
</next_action>