/**
 * telemetry.js — Live Telemetry & Event Feed Panel (placeholder for 08-05)
 *
 * This module will be implemented in plan 08-05.
 * Currently exports a no-op renderer to prevent import errors.
 */

export function renderTelemetry(container, state, api, sse) {
  container.innerHTML = '<div class="panel-placeholder">Telemetry panel — coming in plan 08-05</div>';
}