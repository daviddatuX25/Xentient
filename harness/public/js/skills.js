/**
 * skills.js — Skill Manager Panel (placeholder for 08-04)
 *
 * This module will be implemented in plan 08-04.
 * Currently exports a no-op renderer to prevent import errors.
 */

export function renderSkills(container, state, api, sse) {
  container.innerHTML = '<div class="panel-placeholder">Skill Manager panel — coming in plan 08-04</div>';
}