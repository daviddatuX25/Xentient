/**
 * mode.js — Mode & Space Controls Panel (placeholder for 08-06)
 *
 * This module will be implemented in plan 08-06.
 * Currently exports a no-op renderer to prevent import errors.
 */

export function renderMode(container, state, api, sse) {
  container.innerHTML = '<div class="panel-placeholder">Mode & Space Controls panel — coming in plan 08-06</div>';
}